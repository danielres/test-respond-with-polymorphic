- individual ||= false

- if individual
  = content_tag_for :div, user do
    %h2
      = t('user') + ':'
      = user.email
    .created_at
      = t('date created') + ':'
      = l(user.created_at, :format => :short)

- else
